{
  "name": "anti-doomscroll-webextension",
  "version": "0.1.1",
  "description": "WebExtension to help users avoid doomscrolling.",
  "main": "dist/background.js",
  "scripts": {
    "export": "npm run build; npm run zip",
    "build": "tsc",
    "watch": "tsc --watch",
    "zip": "node scripts/zip.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/firefox-webext-browser": "^120.0.3",
    "@types/node": "^20.13.0",
    "rimraf": "^5.0.7",
    "typescript": "^5.4.5"
  }
}
